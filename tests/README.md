# tests

Cross-app and end-to-end tests (Playwright) — see [CLAUDE.md §30](../CLAUDE.md#30-testing-strategy).

Unit/integration tests for each app live alongside their code (`apps/web`, `apps/api`); this directory holds the E2E suites that span both, starting Phase 3.
